# Changelog

本项目的用户可见变更记录在此。版本标签与安装包见 [GitHub Releases](https://github.com/PDSB001/MiAirX/releases)。

## [Unreleased]

## [1.6.2] - 2026-08-21

### 首次使用与状态

- 新增首次使用向导：管理密码、扫码登录、自动发现音箱、网络设置、检查与完成；旧配置升级不会误触发。
- 新增 `GET /health`，Dashboard 展示 MiAirX、Xiaomi、DLNA、AirPlay、FFmpeg 和音箱在线状态。
- Xiaomi 登录明确区分正常、过期、网络错误和服务暂时不可用；过期时可直接重新扫码。
- 音箱显示 Online / Offline / Unknown，离线不会删除已保存配置。

### 播放与诊断

- Web 播放接口返回稳定错误码，区分来源、音箱、认证、MiNA、转码、格式和网络配置问题。
- 诊断包增加 Python/OS、FFmpeg 版本、Xiaomi、协议、网络、音箱模型和在线状态，并对最近日志再次脱敏。

### 安全与可靠性

- 配置和小米令牌改为私有原子写入；Unix/Linux 下尝试使用目录 `0700`、文件 `0600`。
- 管理台登录增加按直接来源地址计算的失败限流。
- 管理台持久登录有效期由 7 天缩短为 24 小时。
- 修改后台密码后立即注销全部旧会话，当前浏览器也必须使用新密码重新登录。
- 配置文件损坏或违反约束时明确停止启动，不再静默回退默认值。
- 配置、音量和 seek 输入统一使用 Pydantic 校验，非法更新不会部分修改运行状态。

### 媒体与依赖

- 补齐 suffix、open-ended、416 和 `Content-Length` 的 HTTP Range 回归测试。
- 增加异常 Content-Length 和大文件 seek 的内存保护测试，并删除未接入路由的旧媒体代理实现。
- 将 `miservice-fork` 收紧至从已验证版本开始的 2.x 兼容线（`>=2.9.0,<3`）。

### 管理台

- 服务地址展示运行时自动探测出的局域网地址，修复配置留空时显示为 `:8200`。

## [1.6.0] - 2026-08-21

### 新增

- 小米账号扫码登录，成功后自动保存 Cookie 并刷新服务。
- 从小米云设备列表自动发现智能音箱并一键选择。
- 可选的管理台密码保护与七天登录会话。
- 实时日志页面和脱敏诊断包下载。
- GitHub 最新版本检测与手动重新检查。
- 配置保存后的按需热重载；账号、音箱、网络、AirPlay 与音量配置无需手动重启。

### 改进

- 增大媒体下载分块，降低部分媒体的首播等待。
- 局域网地址留空时在使用点自动检测，避免 DHCP 变化后保留旧地址。
- 设置页重新布局，扫码登录、后台安全和版本信息更紧凑。
- Cookie 登录兼容 `miservice-fork` 2.x，并正确保存扫码登录所需的 `serviceToken`。
- 保存后台密码后为当前浏览器重新签发会话，避免立即出现 401。
- 版本检测改用 GitHub `releases/latest` 重定向，避免匿名 API 限额。

### 兼容性

- 现有 `conf/config.json` 可直接升级，新增字段均有默认值。
- 只有 Web 管理端口变更仍需重启 MiAirX；管理台会明确提示。
- AirPlay 仍属于实验能力，DLNA 是当前更稳定的投放路径。

## [1.5.0] - 2026-08-16

- React/TypeScript 管理台与新版 Docker 多阶段镜像。
- 大文件流式媒体代理、SSDP 主动发现和 Docker IPv4 Zeroconf 优化。
- 自动切歌过期任务保护、固定 AirPlay 端口段与完整部署文档。
